{
  "name": "@repo/apollo-ws",
  "version": "4.1.0",
  "description": "WebSocket transport for the feed framework — protocol, subscriptions, and broadcast.",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "transpile": "node ../../scripts/transpile-package.mjs",
    "dev": "node ../../scripts/transpile-package.mjs --watch",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run"
  },
  "author": "Marcin Malinowski <mm.kodmax@gmail.com>",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "@repo/feeds": "*",
    "@repo/logger": "*",
    "ws": "^8.7.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@types/node": "^17.0.35",
    "@types/ws": "^8.5.3",
    "eslint": "^8.57.0",
    "typescript": "5.5.4",
    "vitest": "^4.1.8"
  }
}
