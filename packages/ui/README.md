# @repo/ui

React component library stub from the Turborepo starter (`header`, `counter`, utilities).

## Status

Listed in `apps/web/package.json` but **not used** in the frontend codebase — leftover from `create-turbo`. Shared UI components could be moved here in the future, or the package can be removed.

## Scripts

| Script | Description |
|--------|-------------|
| `lint` / `format` | ESLint / Prettier |

## Stack

React 18, TypeScript — preset `@repo/typescript-config/react-library.json`.
