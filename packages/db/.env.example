# MariaDB credentials for schema migrations (db-migrate).
# Copy to .env — independent from apps/service/.env.
#
# You may use a user with DDL privileges here (CREATE/ALTER/DROP) while
# apps/service/.env keeps a narrower runtime user (SELECT/INSERT/UPDATE).

DB_HOST=
DB_USER=
DB_PASSWORD=
DB_SCHEMA=
