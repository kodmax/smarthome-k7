import styled from '@emotion/styled'

export const Icon = styled('div')({
  backgroundSize: 'contain',
  width: '2em',
  height: '2em',
})

export const DayWrapper = styled('div')({
  flex: '0 0 calc(100% / 7 - 0.4em)',
  padding: '0.2em 0.2em 0',
})
